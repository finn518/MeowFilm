import Hero from "../component/Hero";
import MovieMain from "../component/MovieMain";

const Home = () => {
	return (
		<section>
			<Hero />
			<MovieMain />
		</section>
	);
};

export default Home;
